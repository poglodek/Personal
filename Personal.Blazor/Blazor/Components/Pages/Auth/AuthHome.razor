@page "/auth/"
<PageTitle>Start with Personal App!</PageTitle>

<div class="relative flex flex-col items-center justify-center min-h-screen bg-gray-100 p-4 bg-cover bg-center" style="background-image: url('./img/auth-bg.jpg');">
    <div class="absolute inset-0 bg-black opacity-50"></div>

    <div class="relative z-10 text-white text-center">
        <h3 class="text-xl font-semibold mb-4">Start with Personal App right now!</h3>
        <p class="text-gray-200 mb-6">
            Description: Personal App is an application created for personal trainers and their clients.
            You can create workout plans, design exercises, plan diets, monitor progress, and much more.
            Create an account and take your clients to the next level!
        </p>

        <div class="flex flex-col sm:flex-row justify-center items-center space-y-2 sm:space-y-0 sm:space-x-4 mb-4">
            <NavLink href="/auth/sign-in" class="bg-blue-500 text-white py-2 px-4 rounded-lg hover:bg-blue-600">Sign In</NavLink>
            <NavLink href="/auth/sign-up" class="bg-blue-500 text-white py-2 px-4 rounded-lg hover:bg-blue-600">Sign Up</NavLink>
        </div>

        <div class="mt-2">
            <NavLink href="/auth/set-new-password" class="text-blue-300 hover:underline">Forgot Password?</NavLink>
        </div>
    </div>
</div>
